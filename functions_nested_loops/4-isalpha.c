#include "main.h"

/**
 *
 * _isalpha = checks if a character is alphabetical
 *
 * return = 1 if true
 * return = 0 if false
 */

int _isalpha(int c)
{
	int c


	if (c <= 97 && c >= 122 && c <= 65 && c >= 90)
		return (1)
	else
		return (0)
}
