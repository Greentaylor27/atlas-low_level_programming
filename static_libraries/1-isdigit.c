#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - checks if input is a digit
 * @c: character to check
 * Return: 1 if digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
