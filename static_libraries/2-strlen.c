#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - checks the length of a string
 * @s: string
 * Return: length of the string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
