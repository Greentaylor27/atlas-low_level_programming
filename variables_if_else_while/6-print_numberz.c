#include <stdio.h>
/**
 * main - prints a specific line
 *
 * Return: 0
 */
int main(void)
{
	int digit = 48;
		while (digit <= 57)
		{
			putchar(digit);
			digit++;
		}
		putchar('\n');
		return (0);
}
